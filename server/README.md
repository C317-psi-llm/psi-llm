# Express.js Backend

This template provides a minimal setup to get an Express server running.

The project includes a simple route to verify that the setup is working.

## Running locally

Install dependencies:

```bash
npm install
```

Start the server:

```bash
node index.js
```

The server will run at:

```bash
http://localhost:3000
```
